{
  "version": 2,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.ts",
      "method": ["GET", "POST", "PUT", "DELETE"]
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/app.ts" }]
}
